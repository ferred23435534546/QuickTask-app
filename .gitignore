# quicktask-project/.gitignore

# Dependencias de Node.js (¡nunca subirlas!)
node_modules/
**/node_modules/

# Archivos de build/compilación
dist/
**/dist/
*.js.map
# Comentamos *.js por si algún archivo JS es necesario y no está en dist/node_modules
# *.js

# Archivos de entorno
# Los siguientes archivos .env SÍ se compartirán para este proyecto de clase:
# backend/.env
# db/.env
# Ignoramos un .env genérico en la raíz (si existiera)
.env
# Ignoramos un .env del frontend (si se creara)
frontend/.env

# Logs y archivos temporales
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
report.[0-9]*.[0-9]*.[0-9]*.[0-9]*.json
pids
*.pid
*.seed
*.pid.lock

# Archivos del sistema operativo
.DS_Store
Thumbs.db

# Archivos de editores
# Ignorar todo en .vscode por defecto...
.vscode/*
# ...excepto los archivos de configuración que queremos compartir:
!.vscode/extensions.json
!.vscode/settings.json
# Ignorar otras carpetas de editores comunes
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Archivos de caché de Angular/Tests
.angular/
coverage/

# Archivos específicos de Docker si no los quieres en el repo (opcional)
# .dockerignore # Si usas un .dockerignore separado, podrías ignorarlo aquí.